package engineer.hyper.rag;

public class GetSessionConfig {
    // Placeholder for future query options (e.g., load events, load state)
    public static final GetSessionConfig DEFAULT = new GetSessionConfig();
}